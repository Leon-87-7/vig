## Committing docs to main

When the user explicitly asks to commit to `main` **and** every changed file is a
document (`.md` or `.txt`), just commit them — no extra confirmation needed.

This is a carve-out from the merge policy in `no-merge-to-main.md`: it covers only
direct doc commits the user asked for, not PR merges or non-doc files.
